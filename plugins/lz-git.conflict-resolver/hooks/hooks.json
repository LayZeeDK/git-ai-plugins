{
  "description": "Proactive conflict detection after git merge, pull, and rebase operations",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/detect-conflicts.js",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
